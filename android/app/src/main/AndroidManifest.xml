<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links : ouvrir les liens fami.team / famiteam.com directement dans
                 l'app (au lieu du navigateur) — utilisé pour les liens de connexion par
                 e-mail. android:autoVerify déclenche la vérification automatique par
                 Android via /.well-known/assetlinks.json, à publier sur les deux domaines
                 (voir ce fichier à la racine du site : empreinte SHA-256 à compléter une
                 fois l'app signée). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="fami.team" />
                <data android:scheme="https" android:host="famiteam.com" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- FamiTeam ajoute un rendez-vous quand le parent le demande (bouton
         « Ajouter à mon agenda »), jamais ne parcourt ni n'affiche le
         calendrier existant. READ_CALENDAR est néanmoins requis : sur
         Android, CalendarContract.Calendars (la liste des agendas, utilisée
         pour trouver l'agenda par défaut où écrire) est protégée par cette
         permission, même pour une simple création d'événement — contrairement
         à iOS, qui autorise un accès « écriture seule » au niveau du système.
         Sans elle, la création échoue silencieusement et l'app retombe sur
         le fichier .ics. -->
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
</manifest>
